[package]
name = "bootloader"
version = "0.1.0"
edition = "2021"

[dependencies]
foc-shared = { path = "../shared" }
cortex-m-rt = "0.7"
cortex-m = { version = "0.7", features = ["critical-section-single-core"] }
