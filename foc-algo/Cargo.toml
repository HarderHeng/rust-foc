[package]
name = "foc-algo"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"

[dependencies]
libm = { version = "0.2", default-features = false }

[lib]

[features]
default = ["libm-trig"]
libm-trig = []
